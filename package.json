{
  "private": true,
  "type": "module",
  "name": "esmwell-workspace",
  "description": "Run unbundled ESM in the browser with hard timeouts and normalized results",
  "repository": "https://github.com/jkomyno/runesm.git",
  "author": {
    "name": "Alberto Schiabel",
    "email": "alberto.schiabel@gmail.com"
  },
  "license": "MIT",
  "scripts": {
    "build": "turbo run build",
    "build:watch": "turbo run build:watch",
    "check:exports": "turbo run check:exports",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean",
    "format:ci": "oxfmt . --check",
    "format": "oxfmt .",
    "lint:ci": "pnpm format:ci && oxlint .",
    "lint": "pnpm format && oxlint . --fix --fix-dangerously",
    "lint:fix": "pnpm lint",
    "test": "pnpm -r --if-present test",
    "test:unit": "pnpm -r --if-present test:unit",
    "test:integration": "pnpm -r --if-present test:integration",
    "bench": "turbo run bench",
    "bench:watch": "turbo run bench:watch",
    "changeset": "changeset",
    "version-packages": "changeset version && pnpm install",
    "release:verify": "pnpm lint:ci && pnpm typecheck && pnpm test && pnpm -C packages/esmwell run verify:package && pnpm -C packages/esmwell run verify:browser",
    "release": "pnpm -C packages/esmwell run build:release && changeset publish"
  },
  "devDependencies": {
    "@arethetypeswrong/cli": "catalog:",
    "@changesets/cli": "catalog:",
    "@types/node": "catalog:",
    "lefthook": "catalog:",
    "lint-staged": "catalog:",
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "pkg-pr-new": "catalog:",
    "publint": "catalog:",
    "rimraf": "catalog:",
    "tsdown": "catalog:",
    "turbo": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx,cjs,mjs,cts,mts,json,jsonc,md,yml,yaml}": "oxfmt --check --no-error-on-unmatched-pattern",
    "*.{js,jsx,ts,tsx,cjs,mjs,cts,mts}": "oxlint --no-error-on-unmatched-pattern"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  },
  "packageManager": "pnpm@11.3.0"
}
